_site/
.gh-pages/
# No trailing slash. The browser harness symlinks `node_modules` to a directory outside the
# repository so Playwright resolves, and git sees a symlink as a file — which `node_modules/`
# does not match, so it showed up as untracked on every commit.
node_modules
.DS_Store

# The browser harness. Rebuilt by scratchpad/harness.sh, never committed.
_dash.html
_cfg.html
_shot.html
_stub-db.js
_fonts/
_site/

# The browser harness's own scripts and fixtures.
scratchpad/
